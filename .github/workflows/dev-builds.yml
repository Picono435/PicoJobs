name: Development Builds

on:
  push:
    branches-ignore:
      - translations

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: 17
          distribution: 'adopt'
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: Tests with Gradle
        env:
          STABLE_BUILD: 1
        run: |
          ls
          ./gradlew test
      - name: Publish Unit Test Results
        uses: mikepenz/action-junit-report@v3
        if: success() || failure()
        with:
          report_paths: '**/build/test-results/test/TEST-*.xml'
      - name: Build Bukkit
        env:
          STABLE_BUILD: 0
        run: ./gradlew build :bukkit:build
      - name: Build Fabric
        env:
          STABLE_BUILD: 0
        run: ./gradlew build :fabric:build
      - name: Build Forge
        env:
          STABLE_BUILD: 0
        run: ./gradlew build :forge:build
      - name: Build Nukkit
        env:
          STABLE_BUILD: 0
        run: ./gradlew build :nukkit:build
      - name: Build Sponge
        env:
          STABLE_BUILD: 0
        run: ./gradlew build :sponge:build
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Builds
          path: |
            bukkit/build/libs/*-bukkit.jar
            fabric/build/libs/*-fabric.jar
            forge/build/libs/*-forge.jar
            nukkit/build/libs/*-nukkit.jar
            sponge/build/libs/*-sponge.jar
      - name: Stop The Gradle
        run: ./gradlew --stop
      - name: Gradle Cache
        uses: actions/cache@v3
        with:
          path: |
            .gradle/architectury
            .gradle/loom-cache
            .gradle/8.2.1
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-